[project]
name = "laya-mlx-advisor"
version = "0.1.0"
description = "One local Laya daemon for Codex CLI and Claude Code effort routing"
requires-python = ">=3.11"
dependencies = ["laya-mlx==0.1.0"]

[tool.uv]
package = false
